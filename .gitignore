# Node.js
node_modules/
.env

# Built / deploy artifacts (the dist/ folder is a deployable bundle and is
# intentionally committed so users can grab it directly; the zip is local-only)
openinfra-logger-hostinger.zip

# Rust
rust/target/
rust/Cargo.lock

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
env/
ENV/
env.bak/
venv.bak/
